repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.5.0
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  # NOTE: mypy disabled in pre-commit until #1 is resolved
  # Run manually with: uv run mypy .
  # - repo: https://github.com/pre-commit/mirrors-mypy
  #   rev: v1.8.0
  #   hooks:
  #     - id: mypy
  #       additional_dependencies:
  #         - pydantic>=1.10,<2.0
  #         - boto3-stubs[s3,sqs,cognito-identity]
  #         - types-Pillow
  #         - types-tqdm
